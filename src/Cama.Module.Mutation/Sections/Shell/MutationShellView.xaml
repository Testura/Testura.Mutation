<UserControl x:Class="Cama.Module.Mutation.Sections.Shell.MutationShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:infrastructure="clr-namespace:Cama.Infrastructure;assembly=Cama.Infrastructure"
             xmlns:mutation="clr-namespace:Cama.Module.Mutation"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ContentControl prism:RegionManager.RegionName="{x:Static mutation:MutationModule.MutationOverviewRegionName}" Grid.Row="1" Grid.Column="0" />
        <ContentControl prism:RegionManager.RegionName="{x:Static mutation:MutationModule.MutationDetailsRegionName}" Grid.Row="1" Grid.Column="1" />
    </Grid>
</UserControl>
